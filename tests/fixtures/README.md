# Test Fixtures

Place reusable test data, sample payloads, and recorded API responses in this directory. Pytest will discover files here for parameterized tests and shared fixtures referenced by `tests/conftest.py`.
